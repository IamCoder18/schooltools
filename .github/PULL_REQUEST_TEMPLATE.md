## Summary

<!-- One paragraph or two. What does this PR do and why? -->

## Related issue

<!-- "Fixes #NNN" or "Refs #NNN". Use "n/a" if there's no issue. -->

## Test plan

<!-- How did you verify this works? Be specific. Include commands
     you ran and their output where helpful. -->

## Checklist

<!-- Put an "x" in each box that applies. Delete anything that
     does not apply. -->

- [ ] `go build ./...` is clean.
- [ ] `go test -race ./...` is clean.
- [ ] `go vet ./...` is clean.
- [ ] New code has unit tests next to it.
- [ ] No new top-level dependencies (or justified in the PR body).
- [ ] CHANGELOG.md updated under `[Unreleased]`.
- [ ] No comments added unless asked.
- [ ] Issue opened first for non-trivial work.
- [ ] I have **not** read, written, logged, or printed `.env`,
      `CBE_PASSWORD`, the session file, or any bearer tokens.

## Screenshots / output

<!-- Paste a few lines of CLI output if it helps. Redact
     credentials. NEVER paste CBE_PASSWORD, bearer tokens, or
     full cookie jars. -->

```sh
$
```
